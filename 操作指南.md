# 红石计算机操作指南

## 一、如何编程

&emsp;&emsp;红石计算机所能编程的位置就是带有金块引脚的位置，将红石块放置在金块引脚上代表此位为 1，不放置红石块（空气）则代表此位为 0。指令排布严格按列读取，竖向一列 8 位即为一条完整的机器指令（按列看而非按行看）。所有编程引脚物理上均以 8 列为一组，这并没有什么特殊意义，单纯是因为 3 位二进制 `000 ~ 111` 刚好对应 8 个状态周期，再多一个就会进位。主程序空间的物理最左侧第 1 列即为第 0 列，严格对应程序计数器 PC 地址 `00000`。（其余程序看标识）

&emsp;&emsp;此红石计算机的程序分为主程序、中断程序、自检程序、扩展程序、启动程序、BIOS 程序、上下文切换程序共 7 类。其中主程序（Bank 0）、中断程序（Bank 1）、扩展程序（拓展 Bank）为计算机主要运行的核心程序，其物理位置已在机身顶部显著标出。每一个程序各具备 32 条指令容量，三者共计 96 条指令。

&emsp;&emsp;启动程序和自检程序是出厂预设好的固件，用于带自检启动流程，用户若无特殊需求或硬件底层知识切勿轻易修改。BIOS 程序用于开机选择启动分区，只有选择“带自检启动”才能进入 BIOS；“快速启动”无论控制台是否勾选进入 BIOS，都会默认直接从当前 PC 对应的指令断点开始执行。选择带自检启动时，如果不勾选进入 BIOS，系统自检后会无条件从主程序的第 0 列（PC 地址 `00000`，即第 1 条指令）开始干净执行（无论原来 PC 是多少）；如果勾选进入 BIOS，屏幕会出现分区选择提示：若选择分区 2，会从主程序第 0 列开始执行；若选择分区 1，则会从用户指定的自定义启动地址开始执行。在 BIOS 程序的最后一条指令位置即可自定义该启动地址，该指令物理预置为 `BEQ` 条件跳转指令，修改其后 5 位立即数即为设定的目标启动地址，BIOS 程序的前 7 条指令请勿修改。

&emsp;&emsp;计算机还配备两台上下文切换程序，触发上下文切换程序可以使执行流在主程序与中断程序之间来回切换（详见指令集）。两台切换程序分为主动切换与抢占式切换：其中抢占式切换目前在物理电路上被核间中断（从核 IPI）独占占用，如果程序不需要使用双核协同，可将该中断信号线改接至其他需要中断触发的设备上，例如键盘（已接入备用）、网卡等；主动切换则需要通过 `YIELD` 指令激活。两台上下文切换程序各预设 8 条指令物理槽位，共计 16 条微码容量，但目前硬件上已有 12 条指令槽位被电路屏蔽，想要扩充使用必须修改红石硬件布线，而不能仅仅把程序写在上面。当前硬件下，抢占式切换只支持 `CALL`/`RET` 栈指令机制，主动切换支持 `CALL`/`RET` 机制与无栈协程 `CSW` 指令机制；如果切换现场需要保存内存内容需使用 `DMA` 指令，使用 `CSW` 模式若需保存寄存器内容则需使用 `PUSH`/`POP` 指令，这些高级现场保存均需要修改硬件解锁更多屏蔽槽位后方可使用。

&emsp;&emsp;扩展程序没有特殊限制，正常按列编程即可，在主程序中通过 `EXT` 前缀配合跳转指令即可跨 Bank 切换至扩展程序（详见指令集）。若主程序内没有任何特殊跳转指令，计算机将一直在主程序（Bank 0）的 32 条指令空间内按模 32 循环执行。

&emsp;&emsp;从核的编程规范与主核完全相同，唯一的区别是从核取消了 BIOS 程序，更换成了出厂固化的 MESI 缓存一致性数据同步程序，此程序负责跨核数据自动同步，绝对不要修改。从核物理上依然保留了启动程序和自检程序，但在正常双核协同运行时不会被激活，仅供用户对从核进行独立硬件调试使用。

## 二、操作指南

&emsp;&emsp;编程完毕后，计算机的所有操作均在主控台进行。在非独立调试的正常运行情况下，从核的主控台无需进行任何操作，全流程在主核的主控台操作即可。

&emsp;&emsp;开机前应首先观察主控台上的故障指示灯有无亮起，若故障指示灯处于点亮状态，必须先执行复位操作。复位操作视故障复杂程度不一，若故障灯未熄灭强行开机将得到无法预料的破坏性结果。若常规复位操作无法消除故障灯，请致电电脑维修或直接重新载入地图存档。

&emsp;&emsp;如果程序需要用到显示器，开机前显示器的光标最好处于复位位置，直接点击主控台的复位按钮（光标归首）即可完成复位；若需要清空屏幕旧内容，点击清空屏幕按钮即可（若用户程序开头已编写清屏指令 `01101010` 与复位指令 `01110010`，亦可由程序自动完成而无需手动按键）。此外电脑还配备独立的 4 联自定义画屏，画屏既可以手动点击面板按钮步进显示图像，也可以通过指令 `00101100` 显示图像；画屏图像需要用户前往画屏背面通过红石火把手动自定义，总共可以定义 4 帧独立图元，程序中通过写入 RAM 单元 `111` 的低 2 位来选择目标帧。画屏连续显示 4 次后硬件饱和锁死，清空画屏只能由用户在机身外部手动拉动拉杆清空，计算机内部没有清空或复位画屏的软件指令。画屏和 5 联主显示器属于两块完全独立的屏幕，两者的显示与控制逻辑互不干扰；主显示器的自定义字模帧同样支持用户修改，前往显示器背面改动硬件即可。

&emsp;&emsp;点击快速启动按钮即可完成开机运行。若想要计算机从主程序第 0 列（PC 地址 `00000`，即第 1 条指令）干净启动，需先手动点击 PC 重置按钮，待面板上的 PC 归零指示灯稳定点亮后（建议静置等待 5 秒使红石信号链完全稳定），再点击快速启动按键开机；带自检启动以及勾选进入 BIOS 的引导逻辑已在第一节中详述。计算机运行过程中再次点击快速启动按键即可强制关机。计算机在运行时开机指示灯会保持点亮状态；运行过程中故障指示灯瞬时点亮属于红石中继器信号延时产生的合理过渡现象，但在程序正常执行结束时，所有故障指示灯应当全部处于熄灭状态，合理结束程序运行的方式是主核执行 `HALT` 主动停机或使程序处于预设的无害死循环状态。

&emsp;&emsp;计算机的键盘采用原始二进制输入，3 位二进制物理拉杆直接对应内存单元中的一个 3 位数值。当程序执行到 `LW 110` 轮询指令时，硬件译码器会自动点亮主控台上的“键盘等待输入指示灯”；此时拉下相应数值拉杆，再点击“输入已就绪”按钮即可完成数据提交。当数据被 CPU 读取完毕后，等待输入灯与就绪灯会一并自动熄灭。指示灯熄灭后切勿立刻改动输入拉杆，需要等待 5 秒左右以防输入抖动导致后续读取失败；若等待输入灯亮起而操作员未提交就绪信号，计算机会持续在 `LW 110` 处自旋轮询等待，相当于处于安全的等待死循环中（详见指令集）。

&emsp;&emsp;特别的，在带自检启动且进入 BIOS 选择启动分区的阶段，由于处于底层微码接管期，键盘等待输入指示灯不会自动亮起。观察到屏幕出现 `1 2` 字样时即可进行输入：此时必须先拉下拉杆打开“键盘以中断方式输入开关”，否则键盘信号无法接入总线导致输入无效；设置好分区数值后点击输入已就绪，输入被读取后就绪灯熄灭，同时屏幕上也会回显用户选择的分区（严禁输入除 1 或 2 以外的数字，否则会导致系统进入未定义错误分支）。输入完毕后，必须切记立刻关闭“键盘以中断方式输入开关”推回原位，因为键盘中断线与从核核间中断（IPI）物理共用同一套抢占式中断切换装置，若不关闭该开关，后续正常运行中任何键盘操作都会向 CPU 注入伪 IPI 信号，导致严重的多核死锁崩溃故障。

&emsp;&emsp;雷电接口用于绕过 CPU 快速向内存中批量灌入数据，其物理引脚从右往左分别对应内存单元中的地址 `001 ~ 100`，上方引脚为数据高位，下方为低位，放置红石块代表此位为 1，不放置代表 0。无论引脚是否放置红石块，每次数据传输都会一次性强制覆盖这 4 个内存单元，未放置红石块的对应单元会被直接写成 `000`，绝不保留原内存数据；若想保留原内存数据，可在灌入前将其通过 DMA 备份至硬盘（具体指令见指令集）。特别需要注意的是，雷电接口遵循非一致性 DMA 原理，直接灌入内存绝对不会将硬件脏位置 1；若灌入的数据需要交由从核读取计算，主核在程序开头必须对目标单元显式执行一次读写轻触操作（Touch：`LW 001` 再 `SW 001`）以补上脏位标记，否则从核后续将无法感知数据更新而读取本地旧数据。

&emsp;&emsp;控制台密码锁是用于设置整机总线物理安全校验的硬件门锁，电脑密码在底层电路上已预设固化，严禁随意修改。目前控制台面板上的密码拉杆已处于正确密码对齐状态，如果擅自改动密码，计算机开机时控制总线将被门锁截断，表现为计算得出意料之外的严重逻辑混乱结果，而不是无法开机。

## 三、注意事项

&emsp;&emsp;要想红石计算机顺利运行需要设置区块常加载，正常导入地图的情况下区块常加载设置是保留的，无需额外设置。红石计算机运行大概6秒一条指令，所以看到屏幕更新内容较慢不要心急，程序运行过程中可以先刷手机，等待足够长时间再观察结果。